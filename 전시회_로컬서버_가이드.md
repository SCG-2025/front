# 전시회 로컬 서버 운영 가이드
## 안정적인 Wall 웹 실행을 위한 백업 방안

---

## 🎯 로컬 서버를 사용하는 이유

**원격 서버 문제점:**
- 네트워크 지연으로 인한 p5.js 로딩 순서 문제
- CDN 연결 불안정으로 캔버스 생성 실패
- Firebase 연결 문제
- 인터넷 연결에 의존적

**로컬 서버 장점:**
- 빠른 로딩 속도 ⚡
- 네트워크 독립적 운영 🔒
- 안정적인 캔버스 생성 ✅
- 전시 중 인터넷 끊김 문제 없음 🌐

---

## 💻 전시회 장소 로컬 서버 설정

### 필요 장비
- **노트북 또는 데스크톱 PC** (macOS, Windows, Linux 모두 가능)
- **Git 설치** (코드 다운로드용)
- **간단한 웹서버** (Python, Node.js, 또는 기타)

### STEP 1: 코드 다운로드
```bash
# 전시회 장소에서 실행
git clone https://github.com/SCG-2025/front.git
cd front
```

**Git이 없는 경우:**
1. https://github.com/SCG-2025/front 접속
2. **Code > Download ZIP** 클릭
3. 압축 해제 후 `front` 폴더로 이동

### STEP 2: 웹서버 실행 (여러 방법 중 선택)

#### 방법 1: Python 사용 (가장 간단)
```bash
# Python 3가 설치된 경우
cd front
python3 -m http.server 8000

# 브라우저에서 접속: http://localhost:8000/apps/wall/
```

#### 방법 2: Node.js 사용
```bash
# Node.js가 설치된 경우
npm install -g serve
cd front
serve -s . -p 8000

# 브라우저에서 접속: http://localhost:8000/apps/wall/
```

#### 방법 3: PHP 사용
```bash
# PHP가 설치된 경우
cd front
php -S localhost:8000

# 브라우저에서 접속: http://localhost:8000/apps/wall/
```

#### 방법 4: 전용 서버 프로그램
- **Live Server** (VS Code 확장)
- **XAMPP** (Windows)
- **MAMP** (macOS)

---

## 🖥️ 전시 환경 구성

### 단일 PC 환경
```
[전시용 PC/노트북]
├── 로컬 웹서버 실행 (localhost:8000)
├── 대형 스크린 연결 (HDMI/USB-C)
└── 브라우저: http://localhost:8000/apps/wall/
```

### 다중 모니터 환경 (권장)
```
[메인 PC]
├── 로컬 웹서버 (localhost:8000)
├── 모니터 1: Wall 웹 메인 화면
├── 모니터 2: 미디어아트 화면 1
└── 모니터 3: 미디어아트 화면 2
```

### 네트워크 공유 환경
```
[서버 PC] ─── [공유기/라우터] ─── [관람객 접속용 디바이스들]
    │                                ├── 태블릿 (모바일웹 접속용)
    └── 웹서버 (192.168.1.100:8000)   └── 관리자 노트북
```

---

## 📱 모바일 웹 연동 방법

### 같은 네트워크에서 접속
1. **서버 PC의 IP 주소** 확인:
```bash
# macOS/Linux
ifconfig | grep "inet "
# Windows
ipconfig
```

2. **모바일 디바이스에서 접속**:
```
http://[서버PC IP]:8000/apps/mobile/
예시: http://192.168.1.100:8000/apps/mobile/
```

### 핫스팟 사용
```
[서버 PC] ── [핫스팟] ── [태블릿/스마트폰]
     │         │            └── 모바일웹 접속
     │         └── WiFi 공유
     └── 웹서버 실행
```

---

## 🔧 문제 해결 가이드

### 캔버스가 안 나타날 때
1. **브라우저 새로고침** (Ctrl+F5 또는 Cmd+Shift+R)
2. **브라우저 개발자 도구** 열어서 콘솔 확인:
   ```
   ✅ "p5.js 로딩 완료! main.js 로드 시작"
   ✅ "setup 함수 확인됨"
   ✅ "캔버스 찾음! 드래그 이벤트 설정 시작"
   ```
3. **다른 브라우저** 시도 (Chrome, Firefox, Safari)

### 포트 충돌 문제
```bash
# 다른 포트 사용
python3 -m http.server 8080
# 접속: http://localhost:8080/apps/wall/
```

### 방화벽/보안 문제
- Windows: 방화벽에서 Python/Node.js 허용
- macOS: 시스템 환경설정 > 보안 > 개인정보 보호

### 네트워크 접속 안 될 때
```bash
# 모든 인터페이스에서 접속 허용
python3 -m http.server 8000 --bind 0.0.0.0
```

---

## ⚙️ 최적화 설정

### 브라우저 설정
- **전체화면 모드**: F11 (Windows) 또는 Cmd+Shift+F (Mac)
- **개발자 도구 비활성화**: 일반 사용자 모드
- **자동 업데이트 비활성화**: 전시 중 재시작 방지

### 시스템 설정
- **절전 모드 비활성화**: 화면이 꺼지지 않도록
- **자동 업데이트 비활성화**: 전시 중 방해 방지
- **불필요한 프로그램 종료**: 시스템 리소스 확보

---

## 📋 전시 당일 체크리스트

### 사전 준비
- [ ] **서버 PC 준비** 및 테스트
- [ ] **네트워크 환경 구성** (WiFi/라우터)
- [ ] **모든 디바이스에서 접속 테스트**
- [ ] **백업 USB/하드드라이브** 준비

### 전시 시작 전
- [ ] **로컬 서버 실행**: `python3 -m http.server 8000`
- [ ] **Wall 웹 접속 확인**: `http://localhost:8000/apps/wall/`
- [ ] **모바일 웹 접속 확인**: `http://[IP]:8000/apps/mobile/`
- [ ] **캔버스 정상 작동 확인**

### 전시 중 모니터링
- [ ] **서버 상태 확인** (터미널/명령 프롬프트)
- [ ] **네트워크 연결 상태**
- [ ] **브라우저 오류 없음**

### 비상 계획
- [ ] **백업 PC 준비**
- [ ] **핫스팟 대안 준비**
- [ ] **USB 드라이브 백업**
- [ ] **기술 담당자 연락처**

---

## 🚀 고급 설정 (선택사항)

### Nginx 사용 (고성능)
```nginx
server {
    listen 8000;
    server_name localhost;
    root /path/to/front;
    
    location / {
        try_files $uri $uri/ =404;
    }
}
```

### Docker 사용
```dockerfile
FROM nginx:alpine
COPY . /usr/share/nginx/html
EXPOSE 80
```

### PM2로 프로세스 관리 (Node.js)
```bash
npm install -g pm2
pm2 start "serve -s . -p 8000" --name wall-server
pm2 startup
pm2 save
```

---

## 💡 추가 팁

1. **성능 최적화**: 불필요한 브라우저 확장 프로그램 비활성화
2. **보안**: 전시용 네트워크는 인터넷과 격리 권장
3. **백업**: 전시 중 문제 발생 시 즉시 대체할 수 있도록 준비
4. **테스트**: 전시 전날 현장에서 전체 시스템 테스트 필수

**로컬 서버를 사용하면 안정적이고 빠른 전시 환경을 구축할 수 있습니다!** 🎉